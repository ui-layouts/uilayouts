export const metadata = {
  metadataBase: new URL("https://ui-layouts.com"),

  title: "Image Mousetrail",
  description: "An interactive image mousetrail component where the image responds dynamically to cursor movement, creating a visually engaging interaction. Features smooth mouse tracking, cursor following effects, and fluid image animations for modern interactive experiences.",
  keywords: [
    "Image mousetrail component",
    "Mouse trail effect",
    "Cursor following image",
    "Interactive mouse trail",
    "Mouse tracking component",
    "Cursor trail animation",
    "Image cursor interaction",
    "Mouse following effect",
    "Dynamic mouse response",
    "Interactive cursor effects",
    "Mouse movement tracking",
    "Cursor responsive image",
    "Mouse trail animation",
    "Interactive image component",
    "Cursor interaction effect",
    "Mouse tracking animation",
    "Image mouse effects",
    "Cursor trail component",
    "Mouse responsive design",
    "Interactive mouse component",
    "Cursor following animation",
    "Mouse movement effects",
    "Dynamic cursor interaction",
    "Image trail effect",
    "Mouse cursor component",
    "Interactive cursor trail",
    "Modern mouse effects",
    "UI mouse interaction",
    "React mouse trail",
    "Tailwind mouse effects"
  ],
  authors: [{ name: "Naymur Rahman", url: "https://twitter.com/naymur_dev" }],
  creator: "@naymur_dev",
  publisher: "UI Layouts",
  openGraph: {
    title: "Image Mousetrail Component | UI Layouts",
    description: "An interactive image mousetrail component with dynamic cursor movement response, creating visually engaging interactions and smooth mouse tracking effects.",
    url: "https://ui-layouts.com/components/image-mousetrail",
    images: [
      {
        url: 'https://ui-layouts.com/component-og.jpg',
        width: 1200,
        height: 630,
        alt: 'UI Layouts Components',
      },
    ],
    siteName: "UI Layouts",
    locale: "en_US",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Image Mousetrail Component | UI Layouts",
    images: ['https://ui-layouts.com/component-og.jpg'],
    description: "Interactive image mousetrail with dynamic cursor movement response, creating visually engaging interactions and smooth tracking effects.",
    creator: "@naymur_dev",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  category: "technology",
};

<ComponentCodePreview
  name='image-mousetrail-default'
  jsonName='mousetrail.json'
  hasReTrigger
  isFitheight
/>

## Installation

<Tabs defaultValue="cli">

  <TabsList className='border-b w-full bg-white justify-start dark:bg-neutral-800 h-11'>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>

  <TabsContent value="cli">
   <CodeWithTabs>
      ```js !!tabs ui-layouts
      npx uilayouts@latest add mousetrail
      ```

      ```tsx !!tabs shadcn
      npx shadcn@latest add 'https://www.ui-layouts.com/r/mousetrail.json'
      ```
   </CodeWithTabs>
  </TabsContent>
  
  <TabsContent value="manual" manualType>

<CodeSnippets name="mousetrail" />

</TabsContent>
</Tabs>

I use `data-status` atribute to handle active and inactive item and you can add animation using this attribute:

```jsx
{
  items.map((item, index) => (
    <>
      <img
        key={index}
        className={cn(
          // !mark(1:2)
          // !mark[/active/] pink
          "object-cover  scale-0 opacity:0 data-[status='active']:scale-100  data-[status='active']:opacity-100 transition-transform data-[status='active']:duration-500 duration-300 data-[status='active']:ease-out-expo  absolute   -translate-y-[50%] -translate-x-[50%] ",
          imgClass
        )}
        data-index={index}
        data-status='inactive'
        src={item}
        alt={`image-${index}`}
        ref={refs.current[index]}
      />
    </>
  ));
}
````

Add the following cubic-bazeir to your `tailwind.config.js` file:

```jsx
 transitionTimingFunction: {
  'out-expo': 'cubic-bezier(0.34, 1.56, 0.64, 1)',
 },
```

you can fade out animation when you don't mousemove:

```jsx
const activate = (image, x, y) => {
  setTimeout(() => {
    image.dataset.status = 'inactive';
  }, 1000);
};
```

## Props

| Prop              | Type      | Default     | Description                                                  |
| ----------------- | --------- | ----------- | ------------------------------------------------------------ |
| items             | any[]     | -           | Array of items to display in the mouse trail effect.         |
| children          | ReactNode | undefined   | Optional child elements to render inside the component.      |
| className         | string    | undefined   | Optional CSS class for styling the wrapper of the component. |
| imgClass          | string    | 'w-40 h-48' | CSS class for styling the images in the mouse trail.         |
| distance          | number    | 20          | Distance between each image in the mouse trail.              |
| maxNumberOfImages | number    | 5           | Maximum number of images to display in the trail.            |
| fadeAnimation     | boolean   | false       | Whether to apply fade animation to the images.               |

## Example

### Small Images

<ComponentCodePreview name='image-mousetrail-small' hasReTrigger isFitheight />

### Disappear Images

<ComponentCodePreview
  name='image-mousetrail-disappear'
  hasReTrigger
  isFitheight
/>

### Without Components

<ComponentCodePreview
  name='image-mousetrail-without-component'
  hasReTrigger
  isFitheight
/>
