export const metadata = {
  metadataBase: new URL("https://ui-layouts.com"),

  title: "Randomized Text Effect",
  description:
    "A randomized text effect component for React that animates letters dynamically before revealing the final text. Perfect for creating suspenseful, futuristic, and engaging UI interactions.",
  keywords: [
    "Randomized text effect",
    "Text effect animation",
    "React text animation",
    "Scramble text effect",
    "Glitch text animation",
    "Dynamic text transition",
    "Animated text reveal",
    "Random letter animation",
    "React randomized text",
    "Text scramble effect",
    "Unveil animation React",
    "UI text animation",
    "Futuristic text effect",
    "Random character animation",
    "React text transition",
    "Interactive text animation",
    "Text morphing effect",
    "Dynamic UI typography",
    "Animated text component",
    "React motion text effect",
  ],
  authors: [{ name: "Naymur Rahman", url: "https://twitter.com/naymur_dev" }],
  creator: "@naymur_dev",
  publisher: "UI Layouts",
  openGraph: {
    title: "Randomized Text Effect | UI Layouts",
    description:
      "A randomized text effect component for React that animates characters unpredictably before showing the final message. Great for modern UI, hero sections, and dynamic typography.",
    url: "https://ui-layouts.com/components/randomized-text-effect",
    images: [
      {
        url: 'https://ui-layouts.com/component-og.jpg',
        width: 1200,
        height: 630,
        alt: 'UI Layouts Components',
      },
    ],
    siteName: "UI Layouts",
    locale: "en_US",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Randomized Text Unveil | UI Layouts",
    images: ['https://ui-layouts.com/component-og.jpg'],
    description:
      "A dynamic randomized text effect component that scrambles letters before revealing the final text. Built for React UIs with smooth animations.",
    creator: "@naymur_dev",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  category: "technology",
};


<ComponentCodePreview name='randomized-text-unveil' hasReTrigger isFitheight />

## Installation

```bash
npm install motion @motionone/utils
```

```tsx text-randomized.tsx
'use client';

import React, { useEffect, useState, useCallback } from 'react';

const lettersAndSymbols = 'abcdefghijklmnopqrstuvwxyz!@#$%^&*-_+=;:<>,';

interface AnimatedTextProps {
  text: string;
}

export function RandomizedTextEffect({ text }: AnimatedTextProps) {
  const [animatedText, setAnimatedText] = useState('');

  const getRandomChar = useCallback(
    () =>
      lettersAndSymbols[Math.floor(Math.random() * lettersAndSymbols.length)],
    []
  );

  const animateText = useCallback(async () => {
    const duration = 50;
    const revealDuration = 80;
    const initialRandomDuration = 300;

    const generateRandomText = () =>
      text
        .split('')
        .map(() => getRandomChar())
        .join('');

    setAnimatedText(generateRandomText());

    const endTime = Date.now() + initialRandomDuration;
    while (Date.now() < endTime) {
      await new Promise((resolve) => setTimeout(resolve, duration));
      setAnimatedText(generateRandomText());
    }

    for (let i = 0; i < text.length; i++) {
      await new Promise((resolve) => setTimeout(resolve, revealDuration));
      setAnimatedText(
        (prevText) =>
          text.slice(0, i + 1) +
          prevText
            .slice(i + 1)
            .split('')
            .map(() => getRandomChar())
            .join('')
      );
    }
  }, [text, getRandomChar]);

  useEffect(() => {
    animateText();
  }, [text, animateText]);

  return <div className='relative inline-block'>{animatedText}</div>;
}
```
